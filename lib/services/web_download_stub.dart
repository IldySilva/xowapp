Future<void> downloadBytes(List<int> bytes, String filename) async {
  throw UnsupportedError('Web download is only available in the browser');
}

Future<void> downloadUrl(String url, String filename) async {
  throw UnsupportedError('Web download is only available in the browser');
}
